{
  "name": "stredoskolska-lupa",
  "version": "1.0.0",
  "private": true,
  "description": "Databáze středoškolských podcastů — statický web nasazovaný na Cloudflare Workers",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
